diff --git a/src/common/common/Clock.cpp b/src/common/common/Clock.cpp
index 8d70dc7a..f38f8b5a 100644
--- a/src/common/common/Clock.cpp
+++ b/src/common/common/Clock.cpp
@@ -20,11 +20,20 @@
 
 #include <common/Defines.hpp>
 #include <common/Clock.hpp>
-#include <SDL/SDL.h>
+#include <time.h>
 
-Clock::Clock() 
+// Android build: SDL_GetTicks() replaced with an equivalent monotonic
+// millisecond counter (SDL is not part of this build - see the porting plan).
+static unsigned int getTicksMs()
 {
-	dwLastTime_ = SDL_GetTicks();
+	struct timespec ts;
+	clock_gettime(CLOCK_MONOTONIC, &ts);
+	return (unsigned int)(ts.tv_sec * 1000 + ts.tv_nsec / 1000000);
+}
+
+Clock::Clock()
+{
+	dwLastTime_ = getTicksMs();
 }
 
 Clock::~Clock()
@@ -33,7 +42,7 @@ Clock::~Clock()
 
 unsigned int Clock::getTicksDifference()
 {
-	unsigned int  dwCurrentTime = SDL_GetTicks();
+	unsigned int  dwCurrentTime = getTicksMs();
 	unsigned int  dwTimeDiff = 1;
 	if (dwCurrentTime >= dwLastTime_)
 	{
diff --git a/src/common/common/DefinesScorched.cpp b/src/common/common/DefinesScorched.cpp
index 2e8e15a8..7a71c584 100644
--- a/src/common/common/DefinesScorched.cpp
+++ b/src/common/common/DefinesScorched.cpp
@@ -28,6 +28,9 @@
 #include <common/DefinesAssert.hpp>
 #include <common/Logger.hpp>
 #include <cstring>
+#ifndef _WIN32
+	#include <unistd.h>
+#endif
 
 #ifdef _WIN32
 	#define WIN32_LEAN_AND_MEAN
diff --git a/src/common/common/DefinesString.cpp b/src/common/common/DefinesString.cpp
index 34595ad5..54239c8d 100644
--- a/src/common/common/DefinesString.cpp
+++ b/src/common/common/DefinesString.cpp
@@ -22,8 +22,8 @@
 #include <stdlib.h>
 #include <stdarg.h>
 #include <common/Defines.hpp>
-#include <SDL/SDL.h>
-#include <SDL/SDL_thread.h>
+// NOTE: the upstream SDL includes here are unused by this file's actual
+// code (SDL is not part of the Android build - see the porting plan).
 
 void S3D::trim(std::string &value)
 {
diff --git a/src/common/common/KeyStateTranslate.hpp b/src/common/common/KeyStateTranslate.hpp
index ddb39eac..8c1c2794 100644
--- a/src/common/common/KeyStateTranslate.hpp
+++ b/src/common/common/KeyStateTranslate.hpp
@@ -21,7 +21,7 @@
 #ifndef __INCLUDE_KeyStateTranslate_hpp_INCLUDE__
 #define __INCLUDE_KeyStateTranslate_hpp_INCLUDE__
 
-#include <SDL/SDL_keysym.h>
+#include <SDL_keysym_compat.h>
 
 struct KeyStateTranslation
 {
diff --git a/src/common/common/KeyTranslate.hpp b/src/common/common/KeyTranslate.hpp
index 9a3b7a32..1665bd13 100644
--- a/src/common/common/KeyTranslate.hpp
+++ b/src/common/common/KeyTranslate.hpp
@@ -21,7 +21,7 @@
 #ifndef __INCLUDE_KeyTranslate_hpp_INCLUDE__
 #define __INCLUDE_KeyTranslate_hpp_INCLUDE__
 
-#include <SDL/SDL_keysym.h>
+#include <SDL_keysym_compat.h>
 
 struct KeyTranslation
 {
diff --git a/src/common/common/KeyboardHistory.hpp b/src/common/common/KeyboardHistory.hpp
index 431a3898..3eb8e61c 100644
--- a/src/common/common/KeyboardHistory.hpp
+++ b/src/common/common/KeyboardHistory.hpp
@@ -25,7 +25,7 @@
 //
 //////////////////////////////////////////////////////////////////////
 
-#include <SDL/SDL.h>
+#include <SDL_keysym_compat.h>
 
 namespace KeyboardHistory
 {
diff --git a/src/common/common/Logger.cpp b/src/common/common/Logger.cpp
index 6fce3ae8..fb8174c4 100644
--- a/src/common/common/Logger.cpp
+++ b/src/common/common/Logger.cpp
@@ -21,8 +21,7 @@
 #include <common/Defines.hpp>
 #include <common/Logger.hpp>
 #include <common/LoggerI.hpp>
-#include <SDL/SDL.h>
-#include <SDL/SDL_thread.h>
+#include <pthread.h>
 #include <time.h>
 #include <stdio.h>
 #include <stdarg.h>
@@ -30,10 +29,13 @@
 
 // ************************************************
 // NOTE: This logger is and needs to be thread safe
+// Android build: SDL_mutex/SDL_ThreadID replaced with pthread equivalents
+// (SDL is not part of this build - see the porting plan).
 // ************************************************
 
-static SDL_mutex *logMutex_ = nullptr;
-static Uint32 threadId = (Uint32) -1;
+static pthread_mutex_t logMutex_ = PTHREAD_MUTEX_INITIALIZER;
+static bool haveThreadId = false;
+static pthread_t threadId;
 
 Logger *Logger::instance_ = nullptr;
 
@@ -48,7 +50,6 @@ Logger *Logger::instance()
 
 Logger::Logger()
 {
-	if (!logMutex_) logMutex_ = SDL_CreateMutex();
 }
 
 Logger::~Logger()
@@ -58,16 +59,16 @@ Logger::~Logger()
 void Logger::addLogger(LoggerI *logger)
 {
 	Logger::instance();
-	SDL_LockMutex(logMutex_);
+	pthread_mutex_lock(&logMutex_);
 	Logger::instance()->loggers_.push_back(logger);
-	SDL_UnlockMutex(logMutex_);
+	pthread_mutex_unlock(&logMutex_);
 }
 
 void Logger::remLogger(LoggerI *logger)
 {
 	Logger::instance();
 	std::list<LoggerI *>::iterator itor;
-	SDL_LockMutex(logMutex_);
+	pthread_mutex_lock(&logMutex_);
 	itor = Logger::instance()->loggers_.begin();
 	while(itor != Logger::instance()->loggers_.end()) {
 		if (*itor == logger) {
@@ -76,7 +77,7 @@ void Logger::remLogger(LoggerI *logger)
 		}
 		++itor;
 	}
-	SDL_UnlockMutex(logMutex_);
+	pthread_mutex_unlock(&logMutex_);
 }
 
 void Logger::log(const std::string &text)
@@ -90,15 +91,15 @@ void Logger::log(const char *text)
 
 	Logger::instance();
 
-	SDL_LockMutex(logMutex_);
+	pthread_mutex_lock(&logMutex_);
 	
 	LoggerInfo info;
 	info.setMessage(text);
 	addLog(info);
 
-	SDL_UnlockMutex(logMutex_);
+	pthread_mutex_unlock(&logMutex_);
 
-	if (SDL_ThreadID() == threadId)
+	if (haveThreadId && pthread_equal(pthread_self(), threadId))
 	{
 		processLogEntries();
 	}
@@ -108,9 +109,9 @@ void Logger::log(const LoggerInfo &info)
 {
 	Logger::instance();
 
-	SDL_LockMutex(logMutex_);
+	pthread_mutex_lock(&logMutex_);
 	addLog((LoggerInfo &) info);
-	SDL_UnlockMutex(logMutex_);
+	pthread_mutex_unlock(&logMutex_);
 }
 
 void Logger::addLog(LoggerInfo &info)
@@ -155,14 +156,15 @@ void Logger::addLog(LoggerInfo &info)
 
 void Logger::processLogEntries()
 {
-	if (threadId == (Uint32) -1)
+	if (!haveThreadId)
 	{
-		threadId = SDL_ThreadID();
+		threadId = pthread_self();
+		haveThreadId = true;
 	}
 
 	Logger::instance();
 
-	SDL_LockMutex(logMutex_);
+	pthread_mutex_lock(&logMutex_);
 	std::list<LoggerInfo *> &entries = Logger::instance()->entries_;
 	while (!entries.empty())
 	{
@@ -181,5 +183,5 @@ void Logger::processLogEntries()
 		entries.pop_front();
 		delete firstEntry;
 	}
-	SDL_UnlockMutex(logMutex_);
+	pthread_mutex_unlock(&logMutex_);
 }
diff --git a/src/common/common/RandomGenerator.cpp b/src/common/common/RandomGenerator.cpp
index e0c8c53f..03bcce91 100644
--- a/src/common/common/RandomGenerator.cpp
+++ b/src/common/common/RandomGenerator.cpp
@@ -26,6 +26,10 @@
 #include <net/NetBuffer.hpp>
 #include <stdlib.h>
 #include <stdio.h>
+#include <arpa/inet.h>
+// Android build: SDLNet_Read32 (big-endian/network-order 32-bit read)
+// replaced with the equivalent standard ntohl (SDL is not part of this
+// build - see the porting plan).
 
 RandomGenerator::~RandomGenerator()
 {
@@ -57,7 +61,7 @@ FileRandomGenerator::FileRandomGenerator() : context_( nullptr ), position_( 0 )
 		for (unsigned int i=0; i<bufferSize_; i++)
 		{
 			unsigned int value = tmpbuffer[i];
-			buffer_[i] = SDLNet_Read32(&value);
+			buffer_[i] = ntohl(value);
 		}
 		delete [] tmpbuffer;
 	}
diff --git a/src/common/net/NetBufferPool.cpp b/src/common/net/NetBufferPool.cpp
index d0b2a920..3a1ed4db 100644
--- a/src/common/net/NetBufferPool.cpp
+++ b/src/common/net/NetBufferPool.cpp
@@ -31,26 +31,26 @@ NetBufferPool *NetBufferPool::instance()
 	return instance_;
 }
 
-NetBufferPool::NetBufferPool() : messagePoolMutex_(nullptr)
+NetBufferPool::NetBufferPool()
 {
-	messagePoolMutex_ = SDL_CreateMutex();
+	pthread_mutex_init(&messagePoolMutex_, nullptr);
 }
 
 NetBufferPool::~NetBufferPool()
 {
-	SDL_DestroyMutex(messagePoolMutex_);
+	pthread_mutex_destroy(&messagePoolMutex_);
 }
 
 void NetBufferPool::addToPool(NetBuffer *message)
 {
-	SDL_LockMutex(messagePoolMutex_);
+	pthread_mutex_lock(&messagePoolMutex_);
 	messagePool_.push_back(message);
-	SDL_UnlockMutex(messagePoolMutex_);
+	pthread_mutex_unlock(&messagePoolMutex_);
 }
 
 NetBuffer *NetBufferPool::getFromPool()
 {
-	SDL_LockMutex(messagePoolMutex_);
+	pthread_mutex_lock(&messagePoolMutex_);
 
 	NetBuffer *result = nullptr;
 	if (messagePool_.empty())
@@ -63,7 +63,7 @@ NetBuffer *NetBufferPool::getFromPool()
 		messagePool_.pop_front();
 	}
 	result->reset();
-	SDL_UnlockMutex(messagePoolMutex_);
+	pthread_mutex_unlock(&messagePoolMutex_);
 
 	return result;
 }
diff --git a/src/common/net/NetBufferPool.hpp b/src/common/net/NetBufferPool.hpp
index 58ea9077..e7381a9f 100644
--- a/src/common/net/NetBufferPool.hpp
+++ b/src/common/net/NetBufferPool.hpp
@@ -23,7 +23,9 @@
 
 #include <net/NetBuffer.hpp>
 #include <list>
-#include <SDL/SDL_thread.h>
+#include <pthread.h>
+// Android build: SDL_mutex replaced with pthread_mutex_t
+// (SDL is not part of this build - see the porting plan).
 
 // SINGLETON
 class NetBufferPool
@@ -38,7 +40,7 @@ protected:
 	static NetBufferPool* instance_;
 
 	std::list< NetBuffer* > messagePool_;
-	SDL_mutex*              messagePoolMutex_;
+	pthread_mutex_t         messagePoolMutex_;
 
 private:
 	NetBufferPool();
